# {{ ansible_managed }}
# Remnawave Node environment configuration
# SECRET_KEY is set by the panel when the node is added via Web UI
# Do NOT change SECRET_KEY manually after pairing with the panel

NODE_PORT={{ remnawave_node_port }}
SECRET_KEY={{ remnawave_node_secret_key_value }}
